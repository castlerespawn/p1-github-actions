name: Deployment 1
on: push
jobs:
    deploy :
        runs-on : ubuntu-latest
        steps:
            - name : Get code
              uses : actions/checkout@v3
            - name : Install dependacies
              run : npm ci
            - name : Lint
              run : npm run lint
            - name: Test Code
              run : npm test
            - name: Build Code
              run : npm run build
            - name: Deploy Code
              run : echo "DEPLOY..."

